import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kapesasulok.inventory',
  appName: 'Kape sa Sulok Inventory',
  webDir: 'dist'
};

export default config;
